# Proxy /api/* requests to Render backend (must come before SPA fallback)
/api/*  https://bgc-financialmanagementsystem.onrender.com/api/:splat  200!

# SPA fallback for client-side routing
/*  /index.html  200
